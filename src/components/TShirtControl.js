// import NewTshirtOrder
// import TShirtList from "./TShirtList";
import TShirtDetail from "./TShirtDetail";
import React from "react";
import data from "../data/index";

function TShirtControl() {
  const [formVisibleOnPage, setFormVisibleOnPage] = useState(false);
}


 whenTShirtClicked() = {
 selectedTshirt : props.id;
 return (
  <React.Fragment>
  <TShirtDetail />
  </React.Fragment/>
 )
}

tShirts.push({
  name: doc.data().name,
  artist: doc.data().artist,
  price: doc.data().price,
  quantity: doc.data().quantity,
  size: doc.data().size,
  id: doc.id
});
});
setMainTShirtList(tShirts);
},
error => {
  SpeechSynthesisErrorEvent(error.message);
}
);
return() = unSubscribe();
},[];